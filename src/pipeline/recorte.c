#include "poligonos.h"

Poligono *RecortarParaAreaDeVisao(Poligono *poligono) {
    /*
    lista_entrada <- poligono
    // Recorte à esquerda
    for (reta in poligono) {
        if (p1 fora) {
            if (p2 fora) // ambos fora
                continue;
            guardar intersect, p2  // fora para dentro
        } else {
            if (p2 fora) // dentro para fora
                guarda intersect
            else // ambos dentro
                guarda p2
        }
    }

    lista_entrada <- lista_saida

    // Recorte à direita
    lista_entrada <- lista_saida

    // Recorte acima
    lista_entrada <- lista_saida

    // Recorte abaixo
    lista_entrada <- lista_saida
    // Recorte a frente

    lista_entrada <- lista_saida
    // Recorte atras

    poligono_resultante <- lista_saida
    */
}
